const Titulo = ({titulo, subTitulo}) => {
    //const { titulo, subTitulo} = props destructuring
    return (
        <>
        <h2>{titulo}</h2>
        <h5>{subTitulo}</h5>
        </>
    )
}

export default Titulo